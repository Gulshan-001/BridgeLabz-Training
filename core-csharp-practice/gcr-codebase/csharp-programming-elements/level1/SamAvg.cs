using System;
class HarryAge
{
    static void Main()
    {
        int math=94;
        int phy=95;
        int chem=96;
        Console.WriteLine("Sam's average Mark in PCM is " + (math + phy + chem) / 3);
    }
}